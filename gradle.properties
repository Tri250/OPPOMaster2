# Project-wide Gradle settings.
# 更新时间: 2026-06-22
# 注意：代理配置已移至 ~/.gradle/gradle.properties（用户级）或 CI 环境变量注入
# 主仓库必须零代理配置，避免影响其他开发者 / CI / 真机环境

# ===== 1. Gradle Daemon + JVM 调优 =====
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=384m -XX:+UseParallelGC -XX:+UseStringDeduplication -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvmargs=-Xmx1g -Djava.net.preferIPv4Stack=true
org.gradle.workers.max=4
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.vfs.watch=true

# ===== 2. 构建缓存 =====
org.gradle.caching=true
org.gradle.configuration-cache=false
# 构建缓存 TTL（7 天）
org.gradle.caching.local.ttl.days=7

# ===== 3. Kotlin 编译优化 =====
kotlin.incremental=true
kotlin.daemon.enabled=true
kotlin.compiler.execution.strategy=in-process

# ===== 4. 网络优化配置 =====
# HTTPS 配置
systemProp.https.protocols=TLSv1.2,TLSv1.3
# 优先使用 IPv4，避免 IPv6 连接超时
systemProp.java.net.preferIPv4Stack=true

# 超时配置 - 快速跳过不可用仓库
org.gradle.internal.http.connectionTimeout=15000
org.gradle.internal.http.socketTimeout=30000
systemProp.org.gradle.internal.http.connectionTimeout=15000
systemProp.org.gradle.internal.http.socketTimeout=30000
systemProp.http.connectionTimeout=15000
systemProp.http.socketTimeout=30000
systemProp.https.connectionTimeout=15000
systemProp.https.socketTimeout=30000

# 重试配置 - 减少重试次数
systemProp.org.gradle.internal.network.retry.max.attempts=2
systemProp.org.gradle.internal.network.retry.initial.backOff=500
systemProp.org.gradle.internal.network.retry.max.backOff=2000
systemProp.org.gradle.internal.network.retry.backOff.multiplier=2

# ===== 5. 代理配置 =====
# 使用本地 HTTP 代理访问外网仓库
systemProp.http.proxyHost=127.0.0.1
systemProp.http.proxyPort=18080
systemProp.https.proxyHost=127.0.0.1
systemProp.https.proxyPort=18080
systemProp.http.nonProxyHosts=localhost|127.0.0.1|*.local|*.svc|*.cluster.local

# ===== 6. Android 配置 =====
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.enableResourceNamespaces=true
# AGP 8.9.1 完全支持 compileSdk=36
android.suppressUnsupportedCompileSdk=36

# ===== 7. 安全配置 =====
# 友盟统计 AppKey 已移至 local.properties（已 gitignore），不在版本控制中
# 如需配置，请在 local.properties 中添加：
# UMENG_APPKEY=your_real_appkey
# UMENG_MESSAGE_SECRET=your_real_secret

# Release 签名配置（发布前填入）
# RELEASE_STORE_FILE=release.keystore
# RELEASE_STORE_PASSWORD=YOUR_STORE_PASSWORD
# RELEASE_KEY_ALIAS=omaster
# RELEASE_KEY_PASSWORD=YOUR_KEY_PASSWORD

# ===== 8. 离线模式配置（可选） =====
# 启用离线模式时取消注释（需要先完整构建一次下载所有依赖）
# org.gradle.offline=true

# ===== 9. 日志级别 =====
# 详细日志（调试网络问题时启用）
# org.gradle.logging.level=debug

# ===== 10. 构建性能监控 =====
# 构建时间分析（调试时启用）
# org.gradle.build-time-analysis=true
